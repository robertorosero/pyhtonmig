
/* Code to access structure members by accessing attributes */

#include "structmember.h"

static PyMemberDef $abbrev$_members[] = {
	/* XXXX Add lines like 
        { "foo", T_INT, offsetof($abbrev$$object$, foo), RO },
        */

	{NULL}	/* Sentinel */
};
